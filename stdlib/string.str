
class String {

  def !=(a) {
    if (self == a) {
      "false";
    } else {
      "true";
    }
  }

  def substring(start, end) {
    out = "";
    while (start < end) {
      out = out + self.get_char(start);
      start = start + Counter.new(".");
    }
    out;
  }

}

